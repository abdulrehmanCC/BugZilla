require 'rails_helper'

RSpec.describe "BugsController", type: :controller do


  
  # user = User.create(name: "testq", email: "testq12@example.com", password: "123456", role: "manager")    
  # project = Project.create(name: "projjj", description: "Test Projectdd", user_ids: [user.id])
  # bug = Bug.create(:title => "ffff", :status => 0, :bug_type => 1, :user_id => user.id, :project_id => project.id)
  
  # # bug = Bug.create(title = "BugsHelper", status = 0, bug_type = 1, user = user, project = project)
  # # sign_in user
  # before do
  #   sign_in user
  # end



  # it "Opens a create bug form" do
  #   # sign_in user
  #   get new_project_bug_path(project)
  #   expect(response).to render_template(:new)
  # end

  # it 'Cheking update method of bugs' do
  #   bug.update(title: "asaa")
  #   expect(Bug.find_by(title: "ffff")).to be_nil
  # end


  # it 'Cheking destroy method of bugs' do
  #   bug.destroy
  #   expect(Bug.find_by(title: "ffff")).to be_nil
  # end


  
end
